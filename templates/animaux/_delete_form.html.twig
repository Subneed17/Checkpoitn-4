<form method="post" action="{{ path('animaux_delete', {'slug': animaux.slug}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animaux.id) }}">
    <button class="btn">Supprimer</button>
</form>
   {% for message in app.flashes('success') %}
    <div class="flash-notice" style="text-center">
        {{ message }}
    </div>
{% endfor %}
