{% extends 'base.html.twig' %}

{% block title %}New Animaux{% endblock %}

{% block body %}
    <h1>Create new Animaux</h1>

    {{ include('animaux/_form.html.twig') }}
   {% for message in app.flashes('success') %}
    <div class="flash-notice">
        {{ message }}
    </div>
{% endfor %}
    <a href="{{ path('animaux_index') }}">back to list</a>
{% endblock %}
