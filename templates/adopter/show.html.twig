{% extends 'base.html.twig' %}

{% block title %}Adopter{% endblock %}

{% block body %}
    <h1>Adopter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adopter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ adopter.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ adopter.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img src="{{ vich_uploader_asset(adopter) }}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adopter') }}">back to list</a>

    <a href="{{ path('adopter_edit', {'id': adopter.id}) }}">edit</a>

    {{ include('adopter/_delete_form.html.twig') }}
{% endblock %}
