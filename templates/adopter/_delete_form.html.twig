<form method="post" action="{{ path('adopter_delete', {'id': adopter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adopter.id) }}">
    <button class="btn">Supprimer</button>
</form>
