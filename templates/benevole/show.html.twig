{% extends 'base.html.twig' %}

{% block title %}Benevole{% endblock %}

{% block body %}
    <h1>Benevole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ benevole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ benevole.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ benevole.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ benevole.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('benevole_index') }}">back to list</a>

    <a href="{{ path('benevole_edit', {'id': benevole.id}) }}">edit</a>

    {{ include('benevole/_delete_form.html.twig') }}
{% endblock %}
