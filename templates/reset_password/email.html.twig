<style>
    .message-email-register {
        text-align: center;
        border: 2px solid #fbcf9ba6;
        padding: 1rem;
        width: 60%;
        margin: auto;
    }
    .image-email-register {
        text-align: center;
        padding: 2rem;
        width: 60%;
        margin:auto;
    }
    .button {
        padding: 0.7rem;
        margin:10px 4px;
        color: rgb(187, 65, 17);
        font-family: sans-serif;
        font-weight: 600;
        text-transform: uppercase;
        text-align: center;
        position: relative;
        text-decoration: none;
        display:inline-block;
        border:2px solid rgb(187, 65, 17);
        border-radius: 4rem;
    }
    .button::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        display: block;
        width: 100%;
        height: 100%;
        z-index: -1;
        -webkit-transform: scaleY(.1);
        transform: scaleY(.1);
        transition: all .4s;
        border-radius: 4rem;
    }
    .button:hover{
        color:black;
    }
    .button:hover::before{
        opacity: 1;
        background-color: rgb(187, 65, 17);
        -webkit-transform: scaleY(1);
        transform: scaleY(1);
        transition: -webkit-transform .6s cubic-bezier(.08, .35, .13, 1.02), opacity .4s;
        transition: transform .6s cubic-bezier(.08, .35, .13, 1.02), opacity
    }
</style>
<div class="image-email-register">
    <img style="text-align: center; width:100%;" src="{{ email.image('@images/logo_small.png') }}" alt="Logo Spa de Saintes">
</div>
<div class="message-email-register">
    <h3>Réinitialisation du mot de passe</h3><br>
    <p>Pour réinitialiser votre mot de passe :
        <a class='button' href="{{ url('app_reset_password', {token: resetToken.token}) }}">Réinitialiser le mot de passe</a>
    </p>
    <p>
        Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>
    <br>
    <p>PS: En cas d'absences, vous pouvez contacter le domaine par téléphone ou par mail:</p>
    <p>Numéro de téléphone fixe: <a href="tel:+33546934765"> 05 46 93 47 65</a></p>
    <p>Adresse Mail: <a href="mailto:spa.de.saintes@hotmail.com">spa.de.saintes@hotmail.com</a></p>
</div>
<br></div>
